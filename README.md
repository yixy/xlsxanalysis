角色：你是一个资深Go程序员。
目标：你正在编写一个Go程序，以实现读取指定目录下所有的xlsx文件，并解析相应数据存入本地sqlite数据库的功能。注意，对应配置文件已经写好，参考config.yaml实现具体代码。
要求：1、解析逻辑通过配置文件指定，比如读取第几个sheet的哪些列数据。2、数据库中表有一个文件名称字段，用于区分数据来源于哪个xlsx文件，其值可以通过配置为xlsx某个单元格或者指定为对应文件名。3、数据库表中有一个字段用于记录处理时间。4、程序需要在win7上运行，所以go版本不能高于1.20，选取依赖库的时候需要尤其注意。

//因为sqlite涉及cgo和go的版本兼容性问题，所以需要优化代码，xlsx文件批量处理时，不写入sqlite数据库了，而是直接根据配置文件生成一个合并的xlsx文件。